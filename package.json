{
  "name": "gmaps-lead-generator",
  "version": "2.0.0",
  "description": "Google Maps Lead Generator - Desktop Application",
  "main": "src-tauri/src/main.rs",
  "author": {
    "name": "Your Company",
    "email": "support@yourcompany.com"
  },
  "license": "MIT",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "backend:dev": "cd backend && python -m uvicorn api.server:app --reload --host 127.0.0.1 --port 8000",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4"
  },
  "dependencies": {}
}
